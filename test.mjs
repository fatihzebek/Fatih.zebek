import { initializeApp } from 'firebase/app'; import { getFirestore, collection, getDocs, query, where } from 'firebase/firestore'; async function run() { const app = initializeApp({projectId: 'dh-servis-rapor'}); const db = getFirestore(app); const q = query(collection(db, 'serviceReports'), where('status', '==', 'returned')); const snap = await getDocs(q); console.log('Returned reports count:', snap.size); snap.forEach(d => console.log(d.id, d.data().team)); } run().catch(console.error);
